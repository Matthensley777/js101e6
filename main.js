
// Mathematical operators

var summation = 2 + 3;
var sub = 5 - 1;
var multiplication = 2 * 3;
var divide = 8 / 4;
var modulus = 7 % 2;

//equality operators

// == // values are equal

// === // stri k equality (value are type)

// != // values are not equal

// !== // values are not strictly equal

// relational operators

// > < >= <=

// logical operators

// && (and)

// || (or)

// ! (not)

// conditional logic

var zoeIsCool = true;
var callanIsCool = true;

if (zoeIsCool && callanIsCool) {
	console.log('students are happy');
} else {
	console.log('nope');
}

